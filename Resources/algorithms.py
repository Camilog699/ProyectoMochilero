from Resources.graph import Graph
from Resources.vertex import Vertex


class Algorithms:
    def Prim(self):
        list = []
        graphList = Graph()
        minim = Vertex(min(graphList))
        list.append(minim)
        //TODO
